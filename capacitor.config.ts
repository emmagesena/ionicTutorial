import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.globomantics',
  appName: 'globomantics',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
